#include <stdio.h>
int main() {
    int person's age;
    scanf("%d", &person's age);
    if(person's age >= 18) {
        printf("Eligible");
    }
    else {
        printf("Not Eligible");
    }
    retrun 0;
}